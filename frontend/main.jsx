import React from 'react';
import ReactDOM from 'react-dom/client';
import HomeworkHelper from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HomeworkHelper />
  </React.StrictMode>
);
